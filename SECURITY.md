# Security policy

Security fixes are applied to the latest commit on `main`.

Use GitHub's **Report a vulnerability** link in the Security tab. Do not open
a public issue containing credentials or organizational data.

This is a synthetic portfolio demonstration. Demo personas and headers are not
production authentication; do not expose durable integrations without
enterprise identity, authorization, file scanning, and rate limits.
